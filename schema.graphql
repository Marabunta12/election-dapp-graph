type CandidateAdded @entity {
    id: ID!
    candidateId: BigInt!
    candidateName: String!
}

type Voted @entity {
    id: ID!
    candidateId: BigInt!
}

type ActiveCandidate @entity {
    id: ID!
    candidateId: BigInt!
    candidateName: String!
    votes: BigInt!
}
